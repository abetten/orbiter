The lattice of subsets of a set $X$ is $\frakP(X),$ the set of all subsets of $X$, ordered with respect to inclusion. 
Assume that a group $G$ acts on $X$, and hence on the lattice by means of the induced action on subsets. 
The orbits of $G$ on subsets form a new poset, the poset of orbits. 
Poset classification is the process of computing the poset of orbits. 
Orbiter has an algorithm to perform poset classification. 
In many cases, we are not interested in the full lattice of subsets $\frakP(X)$ 
but rather in a subposet of it.
We require that the subposet is closed under the group action and that the following property holds:
$$
x, y \in \frakP(X) \; \mbox{and} \; x \le y 
\; \Rightarrow \; 
\Big( y \in \cP \rightarrow x \in \cP \Big).
$$
The join of two subsets in the poset may or may not belong to the poset.



Consider the action of the group $G=C_6$ on the set 
$X=\{0,1,\ldots,5\}.$ 
To compute the orbits of $G$ on subsets of $X$, 
we use the following command. 


\sourcecode{poset_of_subsets_under_C6}


The poset of orbits is shown below: 

\orbiteroutput{GRAPHICS/WRAPPERS/poset_of_subsets_under_C6}

The orbits are numbered from $0$ to $13$. 
The elements of one orbit are drawn in groups, 
indicated by a horizontal line. 
The orbits are labeled. 
The label of any orbit has the form $a:b$ where $a$ is the 
number of the orbit and $b$ is the orbit length. 
The orbit numbered 0 is the bottom element of the poset, 
corresponding to the empty set.
The empty set forms an orbit of length $1$.


\bigskip

Next we consider the induced action of $\Sym(4)$ on the set of unordered pairs. 
We use the following command to compute the orbits on subsets. These orbits are in one-to-one correspondence to the isomorphism types of simple graphs of $4$ vertices.


\sourcecode{orbits_on_subsets_in_action_on_pairs}


The poset of orbits is shown below: 

\orbiteroutput{GRAPHICS/WRAPPERS/graphs4_poset_full_lvl_6_draw}


\bigskip



Let us consider the action of the Singer cycle on $\PG(3,2).$
The following command computes the orbits 
of the group $G$ generated by a Singer cycle in $\PG(3,2)$:

\sourcecode{PGL_3_2_singer}


\bigskip

The next command computes the orbits of the projective group $\PGL(4,2)$ 
acting on all subsets of $\PG(3,2)$:

\sourcecode{PG_3_2_subsets}

A diagram showing the poset of orbits is produced:
\orbiteroutput{GRAPHICS/WRAPPERS/PGL_4_2_orbits_on_subsets}



\bigskip

Orbiter can compute orbits of groups acting in various different actions. 
The following example computes the orbits of $\PGL(3,2)$ 
on the subsets of lines of $\PG(2,2).$

\sourcecode{PGL_3_2_on_lines}



\bigskip

The following example computes the orbits of $\PGO(5,2)$ 
on the power set lattice of points of $Q(4,2)$:

\sourcecode{PGO_5_2_on_subsets}

The poset of orbits is shown below: 
\orbiteroutput{GRAPHICS/WRAPPERS/PGO_5_2_poset_orbits_drawing}

