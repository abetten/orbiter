Reed-Solomon codes are BCH-codes where the length $n$ divides $q-1.$
In particular, they are cyclic codes. 
They are almost never binary.

\bigskip

To create a Reed-Solomon code over $\bbF_7$ 
with minimum distance at least $3.$
The command 

\sourcecode{RS_6_3}

creates this code.
The following report is produced:
\orbiteroutput{GRAPHICS/LATEX/RS_q7_6_2_output}
As the report shows, the code is cyclic, generated by 
$$
(X-\alpha)(X-\alpha^2) = (X-3)(X-2)= X^2+2X+6.
$$
Let us investigate this code. 
We start with the weight enumerator. 
The command 

\sourcecode{RS_6_3_weight_enumerator}

computes the weight enumerator, which turns out to be
$$
( 1, 0, 0, 120, 360, 972, 948 ).
$$
In polynomial form, this is
$$
1y^6 + 120x^3y^3 + 360x^4y^2 + 972x^5y + 948x^6.
$$
This confirms that the minimum distance is three.

\bigskip

Let us consider an example of a 
Reed-Solomon code in characteristic two.
We will create a Reed Solomon code of designed distance 3 over $\bbF_8$.
The command 

\sourcecode{RS_7_3}

creates this code.
The following report is produced:
\orbiteroutput{GRAPHICS/LATEX/RS_q8_7_3_output}
As we can see, the code is a cyclic code generated by the polynomial 
$$
(X-\alpha)(X-\alpha^2) = X^2+6X+5.
$$
What is the minimum weight of the code?
The following command 

\sourcecode{RS_7_3_weight_enumerator}

computes the weight enumerator,
which turns out to be 
$$
y^7 + 245x^3y^4 + 1225x^4y^3 + 5586x^5y^2 + 12838x^6y + 12873x^7.
$$
This shows that the minimum distance is 3, as expected.
Computing the automorphism group of the code is computationally infeasible.

\bigskip


The next command performs field reduction of the code. 


\sourcecode{RS_7_3_field_reduction}

This produces a $[21,15]_2$ code.
The reduced matrix is below
\orbiteroutput{GRAPHICS/WRAPPERS/RS_8_red_2_draw}

We store the generator matrix in a makefile variable:

\sourcecodevariable{RS_8_reduced}

Let us compute the weight enumerator of the reduced code.
The command 

\sourcecode{RS_7_3_reduced_weight_enumerator}

computes the weight enumerator of the binary code. It is
\orbiteroutput{GRAPHICS/LATEX/RS_7_3_reduced_weight_enumerator}
In particular, 
the field reduced Reed-Solomon code has minimum distance three. 
Since there are other 
codes of minimum distance 4, the field-reduced code is not optimal.


